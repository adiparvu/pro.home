import SwiftUI

// The Digital Twin no longer uses a map. All map-mode actions (zone styling,
// tap selection, reshape, object drag on the map, zone drawing, camera
// recenter/3D) were removed when the Digital Twin became a static aerial photo
// with directly-placed element pins.
// See DigitalTwinView.swift for the current, self-contained implementation.
