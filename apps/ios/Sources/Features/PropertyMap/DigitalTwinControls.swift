import SwiftUI

// The Digital Twin no longer uses a map. All map-mode controls (layer bar,
// draw/reshape toolbars, heatmap legend, side controls) were removed when the
// Digital Twin became a static aerial photo with directly-placed element pins.
// See DigitalTwinView.swift for the current, self-contained implementation.
